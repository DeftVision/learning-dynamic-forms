import { Box, Typography } from '@mui/material';

const Error = () => {
    return (
        <Box>
           <Typography variant="h5">
               ERROR 404: NOT FOUND
           </Typography>
        </Box>
    );
};

export default Error;